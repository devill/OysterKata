[project]
name = "oyster"
version = "0.1.0"
description = "Billing service for London transport travel"
requires-python = ">=3.11"

[project.optional-dependencies]
test = [
    "pytest>=9.1.1",
    "approvaltests>=19.1.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
